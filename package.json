{
  "name": "yaml-to-code",
  "version": "1.0.0",
  "description": "A small program to read YAML genereted by Swagger and transform it into code",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc || node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.11.7",
    "readline": "^1.3.0",
    "typescript": "^3.6.4",
    "yaml": "^1.6.0"
  }
}
